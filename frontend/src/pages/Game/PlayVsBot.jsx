// PlayVsBot.jsx

// Trang chơi với AI Bot (P2B).

// Dùng cùng GameBoard nhưng logic xử lý trong local, không cần Socket.IO.

// Bot xử lý qua thuật toán Minimax (depth 1–3 tùy độ khó).

// Cho phép Undo/Redo để luyện tập.

// Có thể hiển thị cấp độ bot (Easy / Medium / Hard).