// GameRoom.jsx

// Trang chính khi tham gia phòng chơi thật (P2P).

// Thành phần chính:

// <GameBoard />: bàn cờ realtime.

// <PlayerList />: danh sách người chơi trong phòng.

// <ChatBox />: chat trong phòng.

// Bộ điều khiển: Undo (nếu cho phép), Rời phòng, Âm thanh.

// Lắng nghe Socket.IO:

// Nước đi mới (move), kết thúc trận (gameOver), người rời phòng (playerLeft).

// Có animation, hiệu ứng âm thanh khi đánh cờ.