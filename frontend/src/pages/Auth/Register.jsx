// Register.jsx

// Trang đăng ký tài khoản mới.

// Form: username, email, password, confirm password.

// Gọi API /auth/register → tạo user mới → tự động đăng nhập hoặc chuyển sang /login.

// Có thể kiểm tra điều kiện (password mạnh, email hợp lệ,…).