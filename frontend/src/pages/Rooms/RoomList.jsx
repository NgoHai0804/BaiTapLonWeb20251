// RoomList.jsx

// Danh sách chi tiết các phòng (có thể tách riêng khỏi lobby nếu muốn xem toàn bộ).

// Cho phép join vào phòng (nếu chưa full hoặc có mật khẩu).

// Hiển thị tình trạng: đang chơi / trống / chờ người.

// Có nút “Refresh” để reload danh sách thủ công (ngoài realtime).