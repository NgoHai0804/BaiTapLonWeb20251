// Cell.jsx

// Đại diện cho một ô trên bàn cờ (nếu dùng dạng component-based thay vì Canvas).

// Nhận props như: value (X/O/empty), onClick, highlight.

// Có thể render hiệu ứng hover hoặc animation đánh cờ.