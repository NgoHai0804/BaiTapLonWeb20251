// gameHistory.model.js

// Ghi lại lịch sử các ván đấu.

// Trường dữ liệu chính:

// id, roomId.

// playerXId, playerOId.

// winnerId, moves (danh sách nước đi).

// startedAt, endedAt.

// Dùng để thống kê và hiển thị lịch sử trận đấu.