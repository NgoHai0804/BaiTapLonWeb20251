// index.js

// Gom tất cả route trên vào một router chính.

// Chức năng chính:

// Import từng file route.

// Mount vào prefix, ví dụ:

// app.use('/api/auth', authRoutes);
// app.use('/api/user', userRoutes);
// app.use('/api/room', roomRoutes);


// Giúp app.js gọn gàng, dễ mở rộng.