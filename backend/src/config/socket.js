// socket.js

// Cấu hình và khởi tạo Socket.IO server.

// Nhiệm vụ:

// Tạo io (Socket.IO Server).

// Gắn namespace / middleware xác thực.

// Export io để các module khác (game, chat, friend) dùng.