// validate.middleware.js

// Kiểm tra dữ liệu đầu vào (request body, params, query).

// Chức năng chính:

// Dùng thư viện như Joi hoặc Yup để validate schema.

// Nếu dữ liệu hợp lệ → cho qua controller.

// Nếu sai → trả lỗi 400 Bad Request với chi tiết lỗi.

// Giúp ngăn input sai định dạng hoặc thiếu trường bắt buộc.

